# Usage: blog_preview
# Description:  In your local Octopress root, preview your current octpress site
function blog_preview(){
  rake clean && rake preview
}

# Usage: blog_deploy
# Description: In your local Octopress root, deploy your current octpress site to github
function blog_deploy(){
  rake generate && rake deploy
}